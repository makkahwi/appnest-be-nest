import { Property } from "./property.entity";
import { Modal } from "./modal.entity";
import { Project } from "./project.entity";
import { User } from "./user.entity";

const entities = [Property, Modal, Project, User];

export default entities;
